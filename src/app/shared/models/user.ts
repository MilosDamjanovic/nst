export class User {
    constructor(
        private name: string,
        private surname: string,
        private vocation: string,
        private username: string,
        private password: string,
    ) {}
}
